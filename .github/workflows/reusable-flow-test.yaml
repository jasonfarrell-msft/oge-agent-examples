
on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: xximjasonxx/actions-workflow-repo/.github/workflows/reusable-flow.yaml@main
    with:
      config-path: .github/labeler.yml
    secrets:
      personal_access_token: ${{ secrets.token }}